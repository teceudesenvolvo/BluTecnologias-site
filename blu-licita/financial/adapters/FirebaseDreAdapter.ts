import{collection,getDocs,query,where}from'firebase/firestore';import{httpsCallable}from'firebase/functions';import{db,functions}from'../../../services/firebase';const list=async(n:string,c:string)=>{const s=await getDocs(query(collection(db,n),where('companyId','==',c)));return s.docs.map(x=>({id:x.id,...x.data()}))};export class FirebaseDreAdapter{async load(companyId:string){const[transactions,categories,accounts,closures,projects,costCenters,budgets]=await Promise.all(['financialTransactions','financialCategories','dreAccounts','drePeriodClosures','projects','costCenters','budgets'].map(n=>list(n,companyId)));return{transactions,categories,accounts,closures,projects,costCenters,budgets}}command(value:any){return httpsCallable(functions,'commandDrePeriod')({...value,idempotencyKey:crypto.randomUUID()})}}
