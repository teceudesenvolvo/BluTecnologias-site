import{collection,getDocs,query,where}from'firebase/firestore';import{httpsCallable}from'firebase/functions';import{db,functions}from'../../../services/firebase';
const list=async(n:string,c:string)=>{const s=await getDocs(query(collection(db,n),where('companyId','==',c)));return s.docs.map(x=>({id:x.id,...x.data()}))};
export class FirebaseBudgetAdapter{async load(companyId:string){const[budgets,items,projects,costCenters,transactions,clients,members,companies]=await Promise.all(['budgets','budgetItems','projects','costCenters','financialTransactions','clients','teamMembers','legalEntities'].map(n=>list(n,companyId)));return{budgets,items,projects,costCenters,transactions,clients,members,companies}}command(value:any){return httpsCallable(functions,'commandBudget')({...value,idempotencyKey:crypto.randomUUID()})}}
